** start of script.js **

//saudaçao inicial

console.log("Hello! I'm your coding fun fact guide!");

//2. Criaçao das variaveis do bot

const botName = "Robotec";
const botLocation = "Cloud";
const favoriteLanguage = "JavaScript";

//3. Log de nome e localização

console.log(`My name is ${botName} and I live on ${botLocation}.`);

//4. log da linguagem favorita

console.log(`My favorite programming language is ${favoriteLanguage}.`);

//5 e 6. Criando codingfact com let e exibindo o primeiro fato

let codingFact = `Did you know that ${favoriteLanguage} was create in only 10 days`;
console.log(codingFact);

// 7 e 8. Reatribuindo para o segundo fato e exibindo

codingFact = `A cool thing about ${favoriteLanguage} is that it can be used for both front-end and back-and`;
console.log(codingFact);

// 9 e 10 Reatribuindo para o terceiro fato e exibindo

codingFact = `${favoriteLanguage} is the language that powers the interactivity of almost every website on the planet.`;
console.log(codingFact);

// 11 Mensagem de despedida

console.log(`It was fun sharing these facts with you. Goodbye! - ${botName} from ${botLocation}.`);

** end of script.js **

